
<h2>Lista de categorias:</h2>
<hr>
<a href="./categorias.handlebars"><button class="btn btn-success">Nova categoria</button></a>

{{#each categorias}}
<div class="card">
    <div class="card-body">
        <h4>{{this.nome}}</h4>
        <small>{{this.slug}}</small>
    </div>
</div>
{{else}}
{{/each}}